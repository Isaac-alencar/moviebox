import Routes from "./routes";

// 1. Retrieve TV Shows and mix them in the list together with Movies, sort them by popularity
// 2. Modify necessary components to display the type of the content - `Movie` or `TV Show`
// 3. Add infinite loading instead of pagination buttons

function App() {
    return <Routes />;
}

export default App;
